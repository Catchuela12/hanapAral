# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Increase heap size for the Gradle Daemon
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

# Enable parallel builds and configuration caching to speed up builds
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

# AndroidX and Jetifier
android.useAndroidX=true
# Disabling Jetifier saves memory if you don't use old 'com.android.support' libraries
android.enableJetifier=false

# Kotlin code style
kotlin.code.style=official

# Optimize R class generation
android.nonTransitiveRClass=true
